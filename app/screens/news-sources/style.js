import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	notificationSection: {
		padding: '5%'
	},
	newsSourceView: {
		height: 100,
		backgroundColor: GLOBAL_CONFIG.COLOR.NEWS_BOX_BG_COLOR,
		borderColor: GLOBAL_CONFIG.COLOR.THEME_COLOR,
		borderRadius: 20,
		borderWidth: 1
	}
});

export default styles;
